# Server Modules

This folder is reserved for server-side PiScrow services once demo state is replaced with Supabase-backed routes.

Keep Pi API keys and the Supabase service role key in server-only modules.
